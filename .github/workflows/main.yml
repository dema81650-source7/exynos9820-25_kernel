name: Build Cruel Kernel Note10+ Ultimate

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          ref: cruel
          fetch-depth: 0

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libncurses-dev bc bison flex libssl-dev libelf-dev android-tools-adb git python3 curl wget unzip re2c

      - name: Configure Git Identity
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "GitHub Actions"

      - name: Advanced Python Patching
        run: |
          python3 -c "
          import sys
          import re
          lines = []
          skip_mode = False
          
          try:
              with open('cruelbuild', 'r') as f:
                  for line in f:
                      # Fix the Git Branch check (IndexError & SyntaxError fix)
                      match = re.match(r'^(\s*)branch = run\(', line)
                      if match:
                          indent = match.group(1)
                          skip_mode = True
                          lines.append(f'{indent}branch = \"cruel\"\n')
                          continue
                      
                      # Stop skipping when we reach the end of that multi-line command
                      if skip_mode:
                          if 'splitlines()[1]' in line:
                              skip_mode = False
                          continue
                      
                      lines.append(line)
              
              with open('cruelbuild', 'w') as f:
                  f.writelines(lines)
              print('Patch applied successfully.')
          except Exception as e:
              print(f'Patching error: {e}')
              sys.exit(1)
          "

      - name: Prepare Magisk Environment
        run: |
          mkdir -p usr/magisk/
          echo "26.1" > usr/magisk/magisk_version
          echo "#!/bin/sh" > usr/magisk/update_magisk.sh
          echo "exit 0" >> usr/magisk/update_magisk.sh
          chmod +x usr/magisk/update_magisk.sh
          # Create dummy zip if script checks for it
          touch usr/magisk/Magisk.zip

      - name: Configure and Build Kernel
        run: |
          chmod +x cruelbuild
          # Ensure output directory exists
          mkdir -p out
          
          # Run configuration
          ./cruelbuild config \
            model=N975F \
            toolchain=proton \
            name="Cruel-Hunter-Elite-120FPS" \
            +wireguard \
            +ttl \
            +nohardening \
            +nodebug \
            +ntfs \
            +sdfat \
            +cifs \
            +morosound \
            +boeffla_wl_blocker \
            +force_dex_wqhd \
            +usb_serial \
            +empty_vbmeta \
            +performance \
            +simple_lmk \
            +tcp_bbr \
            +tcp_westwood \
            +io_zen \
            +fp_boost \
            +1000hz
          
          # Build the image
          ./cruelbuild mkimg model=N975F

      - name: Upload Finished Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Cruel-Kernel-Note10Plus
          path: out/*.zip
